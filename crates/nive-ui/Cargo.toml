[package]
name = "nive-ui"
version = "0.1.0"
edition = "2021"
rust-version = "1.92"
license = "Apache-2.0"
description = "Reusable visual design system for Rust/Iced desktop applications."
categories = ["gui"]

[features]
default = []

[dependencies]
iced = { workspace = true }
