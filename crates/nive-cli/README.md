# Nive CLI

`nive-cli` provides the `nive` binary for scaffolding Nive applications and
managing app-owned Lucide icons.

The CLI is intended for framework users and for repository readiness checks that
verify generated apps compile outside the Nive workspace.
